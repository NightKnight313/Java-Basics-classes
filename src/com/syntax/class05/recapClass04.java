package com.syntax.class05;

public class recapClass04 {
	
	if {
		
	}else if {

	} //we use nested if --> when we have 1 condition depend pn another condition 

	if ( false ) {
		code a; 
		
		if ( ture ) {
			code b;
		}
	} 	// line 11 --> line 17 calls statement 

		//if {} , else if {} , else {}  // we can nested if in those blocks 
	if ( false )	{
		code a;
	} else if (ture) {
		code b;
	}else if (ture) {
		code c;
	}		// line 20 --> line 26 one statement 
	
	// example 
	String browser="safari"; // if String always use ( equals )
	
	if (browser.equals("chrome"))	{
		code a;
		}else if (browser.equals("firefox"))	{
			code b;
		}else if (browser.equals("safari"))	{
			code c;
		}
	

}